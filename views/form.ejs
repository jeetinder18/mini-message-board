<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>

        <form method="POST" action="/new">

            <input
                type="text"
                name="messageUser"
                placeholder="Author"
                required
            >

            <input
                type="text"
                name="messageText"
                placeholder="Message"
                required
            >

            <button type="Submit">Send</button>

        </form>

        <%-include('navbar', {links: links}); %>
    </body>
</html>